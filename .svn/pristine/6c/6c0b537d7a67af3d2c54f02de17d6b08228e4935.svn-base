package ch.pschatzmann.starschema.engine;

/**
 * Application abstraction to support alternative caching impleentations
 * @author pschatzmann
 *
 */
public interface ICache {
	Object get(Object key);

	void put(Object key, Object result);
}
